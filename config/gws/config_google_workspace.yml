# Configuración unificada para Google Workspace / Google Drive
# Ruta absoluta en Windows hacia la carpeta de Google Drive que contiene 'documentos'
gdrive_documents_path: 'G:\.shortcut-targets-by-id\1fENbpuTdON265HSA0icoN8nHYS4gWJHF\_rafEl\ishekina'

# Ruta local del enlace dentro del repo (junction/symlink)
local_link_path: 'C:\shekina\services\gws_inteligencia'

# Tipo de enlace a crear en Windows: 'junction' (recomendado) o 'symlink'
link_type: 'junction'

# Asegurar que .gitignore incluya 'documentos/' en la raíz del repo
ensure_gitignore: true
